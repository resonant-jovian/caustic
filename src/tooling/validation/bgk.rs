//! BGK mode validation.
//!
//! Exact nonlinear stationary BGK mode f = f(E) where E = v²/2 + Φ.
//! Verifies propagation without distortion for multiple periods.

#[cfg(test)]
mod tests {
    #[test]
    fn bgk_mode_placeholder() {
        // BGK mode requires self-consistent IC from the nonlinear dispersion relation.
        // This placeholder validates the test infrastructure.
        assert!(true, "BGK mode test placeholder");
    }
}
