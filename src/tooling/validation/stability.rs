//! Jeans stability validation: perturbation below Jeans scale should oscillate, not grow.

#[test]
fn jeans_stability() {
    todo!("same setup but k > k_Jeans; verify amplitude bounded, no growth over many t_dyn")
}
